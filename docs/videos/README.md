# Demo videos

Drop the recorded clips here (referenced from the root README):

- `overview.mp4` — 60-sec product overview (chat → score → seal → verify).
- `troof-score.mp4` — paste a fake SUI → Troof Score **F** → seal → open `/p` → **Simulate tampering** (green→red).
- `verify.mp4` — open a proof on another machine / incognito and watch it verify against the on-chain anchor.

Keep each ≤ ~60s, screen-recorded at 1280×800, no audio needed (voiceover optional). The green→red
tamper flip is the single most important shot — record it unhurried.
